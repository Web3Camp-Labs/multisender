import type {State} from './types'

const INIT_STATE:State = {
    account:null,
    first: null,
    web3Provider: null,
    tips:null,
    txHash: null,
    txHashList: null

};
export default INIT_STATE;